---
// Importar el layout
import DefaultLayout from '../../layouts/DefaultLayout.astro';

// Definir título y rol de usuario
const title = "Gestión de Partos";
const userRole = "administrador"; // Simulación de rol
---

<DefaultLayout title={title} userRole={userRole}>
  <div class="mb-6">
    <h1 class="text-3xl font-bold text-gray-900 dark:text-dark-text">{title}</h1>
    <p class="text-gray-600 dark:text-dark-text-secondary">Registro y seguimiento de los partos de los animales</p>
  </div>

  <div class="mb-6 flex justify-between items-center">
    <div class="flex space-x-2">
      <button class="btn btn-primary flex items-center">
        <span class="mr-1">➕</span> Nuevo Parto
      </button>
      <button class="btn btn-secondary flex items-center">
        <span class="mr-1">🔍</span> Filtros
      </button>
    </div>
    <div>
      <span class="text-sm text-gray-500 dark:text-dark-text-secondary">Total: <span class="font-semibold">0</span> partos</span>
    </div>
  </div>

  <!-- Tabla de partos (placeholder) -->
  <div class="bg-white dark:bg-dark-card rounded-lg shadow overflow-hidden border border-gray-100 dark:border-dark-border">
    <table class="min-w-full divide-y divide-gray-200 dark:divide-dark-border">
      <thead class="bg-gray-50 dark:bg-dark-card-header">
        <tr>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-dark-text-secondary uppercase tracking-wider">Animal</th>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-dark-text-secondary uppercase tracking-wider">Explotación</th>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-dark-text-secondary uppercase tracking-wider">Fecha</th>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-dark-text-secondary uppercase tracking-wider">Género Cría</th>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-dark-text-secondary uppercase tracking-wider">Estado</th>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-dark-text-secondary uppercase tracking-wider">Acciones</th>
        </tr>
      </thead>
      <tbody class="bg-white dark:bg-dark-card divide-y divide-gray-200 dark:divide-dark-border">
        <tr class="hover:bg-gray-50 dark:hover:bg-dark-card-hover">
          <td colspan="6" class="px-6 py-10 text-center text-gray-500 dark:text-dark-text-secondary">
            No hay partos registrados. Agrega uno para comenzar.
          </td>
        </tr>
      </tbody>
    </table>
  </div>
</DefaultLayout>